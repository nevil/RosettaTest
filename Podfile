# Don't send stats of installed pods
ENV['COCOAPODS_DISABLE_STATS'] = 'true'

# OS Deployment Target
$osVersion = 14.0

platform :ios, $osVersion

target 'RosettaTest' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RosettaTest

  target 'RosettaTestTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
